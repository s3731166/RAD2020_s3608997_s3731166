<%= form_for([@post , @post.comments.build]) do |form| %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>
  <br>
 <div class="actions">
    <%= form.submit %>
  </div>
<% end %>